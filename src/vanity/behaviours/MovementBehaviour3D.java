package vanity.behaviours;

import processing.core.PApplet;
import processing.core.PVector;

public class MovementBehaviour3D extends MovementBehaviour {
  public MovementBehaviour3D (PApplet context, PVector origin, PVector scale, PVector direction, PVector speed) {
    super(context, origin, scale, direction, speed);
  }
}
